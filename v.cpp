#include<bits/stdc++.h>
using namespace std;

int main(){
    vector<int>arr;
int val;
    while(1){
        cin>>val;
        if(val<0) break;
        else arr.push_back(val);
    }
    int temp;
	int count=0;
	int i=0;
	while(1){
		if(arr[i] >=100){
			count++;
			temp=arr[i];
			arr[]
		}

	}

	for(auto &val:arr)
	{
		cout<<val<<endl;
	}
}